language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-refactoru-html' ]; then cd .. && eval "mv $currentfolder generator-refactoru-html" && cd generator-refactoru-html; fi

